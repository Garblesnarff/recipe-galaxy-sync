# ===================================
# FEATURE FLAGS
# ===================================
# Copy this file to .env.local and adjust values as needed

# ===================================
# Infinite Scroll Pagination Feature
# ===================================
# Controls whether to use the new infinite scroll pagination (useRecipeDataPaginated)
# or the legacy load-all-at-once approach (useRecipeData)
#
# Options:
# 1. Full Enable:  VITE_FEATURE_INFINITE_SCROLL=true
# 2. Full Disable: VITE_FEATURE_INFINITE_SCROLL=false
# 3. A/B Testing:  Leave VITE_FEATURE_INFINITE_SCROLL unset and use VITE_INFINITE_SCROLL_ROLLOUT_PERCENT
#
# Example configurations:
# - Production safe rollout: VITE_FEATURE_INFINITE_SCROLL=false
# - Testing in staging: VITE_FEATURE_INFINITE_SCROLL=true
# - 10% user rollout: Leave blank and set VITE_INFINITE_SCROLL_ROLLOUT_PERCENT=10

# Enable/disable infinite scroll (overrides percentage rollout if set)
# Default: false (safe rollout)
VITE_FEATURE_INFINITE_SCROLL=false

# A/B Testing: Percentage of users to enable feature for (0-100)
# Only applies if VITE_FEATURE_INFINITE_SCROLL is not explicitly set to true/false
# Uses stable user ID stored in localStorage for consistent experience
# Examples:
#   0   = Disabled for all users
#   10  = Enabled for 10% of users
#   50  = Enabled for 50% of users
#   100 = Enabled for all users
# Default: 0 (disabled)
VITE_INFINITE_SCROLL_ROLLOUT_PERCENT=0

# Number of recipes per page when pagination enabled (default: 20)
# Adjust this to optimize performance for your use case
# Recommended range: 10-50
VITE_PAGINATION_SIZE=20

# ===================================
# MONITORING & DEBUGGING
# ===================================

# Enable performance monitoring (default: true in dev, false in prod)
# Logs performance metrics for page loads, queries, and user interactions
VITE_ENABLE_PERFORMANCE_MONITORING=true

# ===================================
# SUPABASE CONFIGURATION
# ===================================

VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
